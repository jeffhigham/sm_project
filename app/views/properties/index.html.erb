<table>
  <tr>
    <th>Profile photo</th>
    <th>Address</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @properties.each do |property| %>
  <tr>
    <td><%= link_to image_tag(property.driveway_type.image_url), edit_property_path(property) %></td>
    <td><%= property.address %></td>
    <td>
      <%= link_to 'Move My Snow!', new_property_work_order_path(property), :class => "radius button right" %>
    </td>
  </tr>
<% end %>
</table>
<br />
<div>
<%= link_to 'New Location', new_customer_property_path(current_customer), :class => "radius button left" %>
</div><p/>